# Library-Management-System-
This Library Management System is developed using Core Java, JDBC, and MySQL. It follows OOP principles and supports adding, updating, searching, issuing, and returning books. JDBC is used for database connectivity, while MySQL stores and manages library records efficiently through CRUD operations.
